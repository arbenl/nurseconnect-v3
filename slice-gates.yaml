# Slice gate manifest (per SOP Step 3; machine-enforced from NC-EG-01 onward)
slice: NC-EG-00
branch: codex/constitution-deployment
gates:
  ent-tm:
    status: n/a
    justification: >-
      Pre-NC-EG-02 (no threat-model gate exists yet). No auth, session, PHI,
      tenancy, or money surface is touched. The one gate-integrity-relevant
      change (finalizer tracker-ID regex extension) is disclosed in the PR
      body, fail-closed on unknown bands, regression-tested, and covered by
      the security_reviewer receipt in the verify-slice run root.
  ent-dlv:
    status: n/a
    justification: >-
      No database schema, migration, or data-bearing column is added or
      modified; the diff is governing documentation plus a CI validator regex
      and its test.
  ent-perf:
    status: n/a
    justification: >-
      No runtime code path, critical route, or bundle is touched; CI-only
      validator change.
