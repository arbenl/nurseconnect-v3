{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*"],
  "globalEnv": [
    "NODE_ENV",
    "FIRST_ADMIN_EMAILS",
    "APP_URL",
    "BETTER_AUTH_URL",
    "BETTER_AUTH_SECRET",
    "DATABASE_URL",
    "DATABASE_POOL_URL",
    "PGPOOL_MAX",
    "PGPOOL_MIN",
    "PGPOOL_IDLE_TIMEOUT_MS",
    "PGPOOL_CONNECTION_TIMEOUT_MS",
    "PGPOOL_MAX_LIFETIME_SECONDS",
    "PGPOOL_ALLOW_EXIT_ON_IDLE",
    "SKIP_ENV_VALIDATION",
    "E2E_TEST_MODE",
    "FEATURE_BACKEND_NURSE_PROFILE",
    "FEATURE_BACKEND_PATIENT_PROFILE",
    "FEATURE_BACKEND_SERVICE_REQUEST",
    "FEATURE_BACKEND_ASSIGNMENT",
    "FEATURE_BACKEND_VISIT"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "type-check": {
      "dependsOn": ["^build"]
    }
  }
}
