> **Archive notice:** This file records the M0-M14 stabilization history and is
> no longer the active milestone tracker. Use the
> [Launch Readiness Review Runbook](runbooks/launch_readiness_review.md) and the
> [M15 Program Roadmap Lock Design](superpowers/specs/2026-04-24-program-roadmap-lock-design.md)
> for current launch and post-M14 sequencing.

# NurseConnect V3 v1.0.0 Plan (RED -> AMBER -> GREEN)

## Current status after M14 and Sonar quality-gate parity

This file records the original RED -> AMBER -> GREEN stabilization sequence.
It remains useful as launch hardening history, but it is no longer the current
milestone tracker.

The completed v1.0.0 program sequence is M0 through M14:

- M0 architecture spine complete
- M1 referral partner MVP
- M2 triage and exception model
- M3 private-pay and payout traceability
- M4 service-area controls
- M5 launch readiness review
- M6 automated launch rehearsal
- M7 full milestone browser rehearsal
- M8 production monitoring and alerting
- M9 roadmap reconciliation and next-slice program plan
- M10 first-hour production synthetic monitoring
- M11 auth and session degradation monitoring
- M12 launch operator console hardening
- M13 controlled launch execution readiness review
- M14 rehearsal browser hardening

SonarCloud PR quality-gate parity was also completed after M14 as an
infrastructure parity slice. It is intentionally not a product milestone, but it
is now part of the required PR merge discipline.

The next numbered program sequence is locked by the
[M15 Program Roadmap Lock Design](superpowers/specs/2026-04-24-program-roadmap-lock-design.md):

- M15 program roadmap lock
- [M16 CRM boundary design](superpowers/specs/2026-04-25-crm-boundary-design.md)
- M17 controlled launch dry run and decision ledger
- M18 CRM read-only operator view
- M19 CRM notes and follow-ups

CRM is not a v1.0.0 launch prerequisite. It must start with M16 boundary design
before any CRM tables, routes, or UI are built. Controlled launch dry-run
evidence remains ahead of CRM implementation.

The current launch decision package is the
[Controlled Launch Execution Readiness Runbook](runbooks/controlled_launch_execution_readiness.md).
Program source of truth remains Notion plus the repository specs and runbooks.

## Scope
- Stabilize core launch blockers with minimal PRs and deterministic CI.
- Restore build/type integrity for `web`.
- Close authz/data-exposure gaps (`/api/user`, RBAC consistency, 401/403/404 policy).
- Enforce clean-database reproducibility as authoritative for DB/API lanes.
- Make `/api/me` failure semantics truthful and safely handled by clients.
- Add hard, testable release gate command for merge readiness.

## Non-scope
- Broad UI redesign/polish beyond required error-state compatibility for `/api/me`.
- New product features outside onboarding/request lifecycle hardening.
- Full observability platform expansion (SLO dashboards, tracing rollout) for this phase.

## PR sequence
1. PR-0: `chore(ci): align pnpm + env validation`
2. PR-1: `fix(web): request-events typing + /api/me truthful failures`
3. PR-2: `security(api): close /api/user leak + RBAC policy matrix`
4. PR-3: `db(nurse): UNIQUE(user_id) + idempotent become-nurse`
5. PR-4: `ci(db): clean DB authority + one-command release gate`

## PR plan

| PR | Goal | Files likely touched | Approach | Risks | Acceptance criteria | Proof |
|---|---|---|---|---|---|---|
| PR-0 | Deterministic preflight in CI/local | See details below. | Align pnpm and env validation. | Env checks can initially fail noisy on missing vars. | CI uses the repo pnpm major/minor; env validation fails fast with actionable messages. | `pnpm env:check`; CI `quality` |
| PR-1 | Restore build and honest `/api/me` error contract | See details below. | Fix request-event typing and make `/api/me` failure semantics truthful. | Client regressions if consumers assume `ok:true` always. | Dashboard shows a retry-safe unavailable state; `useUserProfile` avoids loops/crashes; web build/typecheck pass. | `pnpm -w type-check && pnpm --filter web build` |
| PR-2 | Close `/api/user` leak and normalize RBAC responses | See details below. | Gate `/api/user` by policy, standardize authz mapping, and review usage before removal. | Breaking consumers if route usage exists and is not migrated first. | Review `rg "api/user" -n apps/web`; migrate usage first when present; do **not** hard-delete the route before proof; cover 401/403/404 in matrix tests. | `rg "api/user" -n apps/web`; `pnpm gate:e2e-api` |
| PR-3 | Enforce single nurse profile per user and race-safe onboarding | See details below. | Align schema/migration uniqueness and use conflict-safe onboarding writes. | Dedup step removes redundant historical duplicates by policy. | Clean migrate passes; duplicate nurse rows cannot be created; repeated become-nurse remains idempotent. | `pnpm db:from-clean`; `pnpm gate:e2e-api` |
| PR-4 | Make clean DB reproducibility authoritative and guard resets | See details below. | Require clean DB setup before DB/API tests and add `pnpm gate:release`. | Weak reset guardrails could damage non-test data. | Reset refuses non-test URLs unless explicitly overridden; DB/API lanes start from clean migrate; release gate is deterministic. | `pnpm gate:release`; CI `db-integration`, `e2e-api`, `e2e-ui-smoke` |

<details>
<summary>PR plan file detail</summary>

- PR-0 files: [.github/workflows/ci.yml](../.github/workflows/ci.yml),
  [package.json](../package.json), [scripts/env-check.mjs](../scripts/env-check.mjs),
  and historical `scripts/env-validate.mjs`.
- PR-1 files: historical `apps/web/src/server/requests/request-events.ts`,
  [apps/web/src/server/requests/request-events.db.test.ts](../apps/web/src/server/requests/request-events.db.test.ts),
  [apps/web/src/app/api/me/route.ts](../apps/web/src/app/api/me/route.ts),
  [apps/web/src/types/me.ts](../apps/web/src/types/me.ts),
  [apps/web/src/hooks/use-user-profile.ts](../apps/web/src/hooks/use-user-profile.ts),
  [apps/web/src/app/(app)/dashboard/dashboard-client-page.tsx](<../apps/web/src/app/(app)/dashboard/dashboard-client-page.tsx>).
- PR-2 files: historical `apps/web/src/app/api/user/route.ts`,
  [apps/web/src/server/auth/require-role.ts](../apps/web/src/server/auth/require-role.ts),
  [apps/web/src/server/auth/http.ts](../apps/web/src/server/auth/http.ts),
  [apps/web/src/server/auth/index.ts](../apps/web/src/server/auth/index.ts),
  request/location route handlers, and
  [apps/web/tests/e2e-api/authz.api.e2e.ts](../apps/web/tests/e2e-api/authz.api.e2e.ts).
- PR-3 files: [packages/database/src/schema/nurses.ts](../packages/database/src/schema/nurses.ts),
  `packages/database/drizzle/*0009*`,
  [apps/web/src/app/api/me/become-nurse/route.ts](../apps/web/src/app/api/me/become-nurse/route.ts),
  [apps/web/src/app/api/me/nurse/route.ts](../apps/web/src/app/api/me/nurse/route.ts),
  and [apps/web/tests/e2e-api/nurse.api.e2e.ts](../apps/web/tests/e2e-api/nurse.api.e2e.ts).
- PR-4 files: [scripts/db-reset-schema.sh](../scripts/db-reset-schema.sh),
  [package.json](../package.json), and
  [.github/workflows/ci.yml](../.github/workflows/ci.yml).

</details>

## Go/no-go checklist

This archive no longer owns the launch checklist. See the
[Launch Readiness Review go/no-go checklist](runbooks/launch_readiness_review.md#go-no-go-checklist).

## Release gate (single command)

```bash
pnpm gate:release
```

## Git hooks

- Install hooks once per clone:
  - `pnpm hooks:install`
- `.githooks/pre-commit` blocks commits when unstaged or untracked files exist.
- `.githooks/post-commit` enforces a clean tree immediately after commit.
- `.githooks/pre-push` requires a clean tree, runs `pnpm gate:release`,
  re-checks the tree, and blocks unresolved Copilot/Sentry review threads.
- Emergency local bypasses: `STRICT_GUARD_ALLOW_UNTRACKED=1` for the commit
  hook only, and `STRICT_GUARD_SKIP=1` to skip a hook run.

## CI enforcement

- `pr-finalizer` validates required CI checks are green and review
  conversations are resolved.
- `pr-finalizer` runs
  [scripts/check-pr-bot-comments.mjs](../scripts/check-pr-bot-comments.mjs) to
  block unresolved Copilot/Sentry review threads.

## Definition of done
All required CI lanes green on `main` + clean DB reproducibility passes.
