# Database
# Default points to local Docker Postgres
DATABASE_URL="postgresql://nurseconnect:nurseconnect@localhost:5432/nurseconnect"
DATABASE_POOL_URL="" # Optional: Pooled connection URL for the app runtime.
PGPOOL_MAX="" # Optional (int): Max number of clients in the pool (e.g., 10)
PGPOOL_MIN="" # Optional (int): Minimum number of clients in the pool
PGPOOL_IDLE_TIMEOUT_MS="" # Optional (int): Idle ms before closing connection (e.g., 5000 for serverless)
PGPOOL_CONNECTION_TIMEOUT_MS="" # Optional (int): Connection timeout in ms
PGPOOL_MAX_LIFETIME_SECONDS="" # Optional (int): Max lifetime of connection in seconds
PGPOOL_ALLOW_EXIT_ON_IDLE="" # Optional (boolean): Allow exit on idle

# Auth (Better-Auth)
BETTER_AUTH_SECRET="dev-secret-do-not-use-in-prod"
BETTER_AUTH_URL="http://localhost:3000"
FIRST_ADMIN_EMAILS="" # Optional: comma-separated list of emails to bootstrap to admin. Leaving empty = safe default (no admins created).
